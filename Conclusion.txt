Вывод.

Исходя из полученных значений и диаграмм можно предположить, что при использовании больших объёмов данных исследуемые коллекции ведут себя неодинково.
В операции добавления элемента в коллекцию выделяется HashSet, показавший большее время выполнения на операции add(Element e).
Методы clear() и iterator.next() показали слишком большое время выполнения у вручную написанной коллекции CustomList.
Метод remove(Element e) выделил коллекцию ArrayList на фоне остальных исследуемых.

В качестве вывода имеет основание предположение:
Коллекция CustomList показала себя неэффективной на наиболее часто выполняемой операции iterator.next(), что характеризуем её как не предпочтительную.
Для временного хранения данных с необходимостью доступа, а затем полного очищения коллекции лучше всего использовать ArrayList.
LinkedList и HashSet показали себя приблизительно одинаково на тестовых значениях, соответственно в данный момент сложно предположить эффективные случаи их использования относительно данных исследуемых данных.